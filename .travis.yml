dist: bionic
language: python

python:
  - '3.6'

addons:
 
  firefox: latest

install:
  - pip install pipenv
  - pipenv install --dev

before_script:
 
  - install-webdrivers

env:
  global:
    - DJANGO_SETTINGS_MODULE="pur_beurre.settings"

services:
  - postgresql

script:
  - python manage.py test 