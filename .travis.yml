dist: bionic
language: python

python:
  - '3.7.5'

addons:
  # ajouter uniquement les navigateurs utilisés
  firefox: latest

branches:
  only:
    - staging
    
install:
  - pip install pipenv
  - pipenv install --dev
  - install-webdrivers

services:
  - postgresql

script:
  - python manage.py test -v2